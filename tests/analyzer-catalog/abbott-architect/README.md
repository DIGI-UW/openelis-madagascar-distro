Synthetic MLLP regression traffic covers every retained profile code across saved restart without a per-analyzer port. A control-looking accession must remain NOT_EVALUATED because this profile explicitly has no configured recognition rules. This preserves prior unclassified behavior; it is not physical instrument or clinical validation.
